services:
  app:
    build:
      context: .
      args:
        APP_VERSION: "0.1.0"
        GIT_COMMIT: "${GIT_COMMIT:-unknown-local}"
    ports:
      - "8000:8000"
    env_file:
      - .env
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
